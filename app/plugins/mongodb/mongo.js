const project = require('./mongo.project');
const user = require('./mongo.user');
const fabric = require('./mongo.fabric');
const block = require('./mongo.block');

module.exports = {
  project,
  user,
  fabric,
  block,
};

